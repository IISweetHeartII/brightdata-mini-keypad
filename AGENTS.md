# AGENTS.md

이 레포에서 AI 에이전트가 지켜야 할 규칙. 사람용 사용법은 [`README.md`](./README.md).

## 이 레포가 하는 일

Bright Data 굿즈 3키+노브 매크로 키패드(WCH CH57x, USB `1189:8890`)의 키 매핑을
프리셋 단위로 관리한다. 물리 하드웨어의 온보드 메모리를 쓰는 도구다.

```
kp                 프리셋 목록/굽기 스크립트 (bash, 의존성 없음)
presets/*.yaml     프리셋. 파일을 추가하면 kp 목록에 자동 등장한다
reference/         업스트림 예제 (수정하지 말 것)
docs/              사진, 세팅 프롬프트
.current           마지막으로 구운 프리셋 이름 (git 미추적)
```

## 🔴 하드웨어 안전 규칙

**아래 조합을 프리셋에 넣지 마라. 사용자가 요청해도 결과를 먼저 알리고 확인을 받아라.**

| 조합 | 결과 |
|---|---|
| `ctrl-cmd-power` | 저장 없이 **강제 재시동** — 실행 중인 모든 작업 사망 |
| `shift-cmd-q` | **로그아웃** — 세션의 모든 프로세스 사망 |

화면 잠금은 `ctrl-cmd-q` 다. 로그아웃 `shift-cmd-q` 와 끝 글자가 같아 실제로 혼동 사고가 났다.
`power` 가 들어간 조합을 쓸 때는 모디파이어를 특히 주의해서 검토하라.

## 검증 규칙

**`upload` 의 `exit=0` 은 "기기에 썼다"까지만 증명한다. "원하는 동작을 한다"는 증명하지 않는다.**

이 레포에서 "동작한다"고 말하려면 사람이 실제로 키를 눌러본 결과가 있어야 한다.
사람의 확인 없이 완료를 선언하지 마라. 확인을 못 받았으면 무엇이 미검증인지 명시하라.

기기에서 설정을 **되읽을 수 없다**(도구에 download 명령이 없다). 지금 무엇이 굽혀 있는지
추론하지 말고 `.current` 를 읽어라. 그것도 기록일 뿐 기기의 실제 상태가 아니다.

물리 키 순서(위·중간·아래 ↔ 키1·키2·키3)는 **문서에 없다.** 추측하지 말고 `./kp identify`
를 굽고 사람에게 눌러보게 하라.

**`validate` 통과는 기기가 받아준다는 뜻이 아니다.** 실측 사례: 키 시퀀스는 5키가 상한인데
`validate` 는 30키도 "valid" 로 통과시키고, 6키부터 `upload` 가 `Error: bind key` 로 거부한다.
새 문법을 제안하기 전에 `validate` 가 아니라 `upload` 로 확인하라.

## 매핑을 정하기 전에

사용자 환경을 **조사하고** 정하라. 추측 금지.

- 이미 쓰는 스크린샷/유틸 앱: `ps -axo comm= | grep -iE "shottr|cleanshot|monosnap|xnip"`
  있으면 그 앱의 설정 파일에서 **실제 핫키 조합**을 읽어라
  (예: Shottr 는 `defaults read cc.ffitch.shottr` 의 `KeyboardShortcuts_*`,
  값은 carbon modifier 비트마스크 — `2048`=⌥, `512`=⇧, `4096`=⌃, `256`=⌘)
- macOS 기본 단축키: `com.apple.symbolichotkeys` plist
- 이미 마우스가 커버하는 기능(미션 컨트롤, 데스크탑 전환)은 중복이니 제외를 제안하라

키가 3개뿐이다. 자리를 낭비하는 매핑(키보드 홈포지션이 더 빠른 `cmd-c` 류)은 근거를 들어 반대하라.

## 함정 (검증됨 — 다시 밟지 마라)

1. **Rust 도구다. Python 아니다.** `pip install -r requirements.txt` /
   `ch57x-keyboard-tool.py` 는 업스트림에 없다(404 확인).
2. **`minikeyboard.top` · `sayodevice.com` · `key.soku.cc` 는 이 기기를 못 잡는다.**
   SayoDevice 는 다른 회사의 다른 프로토콜이다. 여기서 시간 쓰지 마라.
3. **`validate`/`upload` 는 파일 인자를 안 받는다 — stdin 전용.**
4. 3키 모델은 **시퀀스의 첫 키에만** 모디파이어가 붙는다.
   시퀀스 상한은 **5키**이고 미디어키는 시퀀스에 넣을 수 없다.
   따라서 `/compact` 같은 텍스트 매크로는 불가능하다 — 긴 매크로는 Raycast 등
   런처의 전역 핫키로 빼고 키패드는 그 핫키만 쏘게 설계하라.
5. macOS "키보드 식별 마법사"는 하드웨어와 무관하다. 닫아도 된다.

## 스크립트를 고칠 때

`kp` 는 의존성 없는 bash 다. 고쳤으면 해피 패스만 보지 말고 **가드 레일을 실사격하라.**

```bash
./kp                      # exit 0, 목록
./kp nonexistent          # exit 1, 에러 + 목록
(cd /tmp && /절대경로/kp)  # 다른 cwd 에서도 경로 해석이 깨지지 않는가
./kp keys volume          # exit 0
```
